#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alpha;

for (alpha = 'z'; alpha >= 'a'; alpah--)
{
putchar(alpha);
}
putchar('\n');
return (0);
}
